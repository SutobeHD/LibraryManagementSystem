# --- Multi-Platform Release Workflow ---
# Trigger: Push of any tag matching `v*` (e.g. v0.0.3Alpha, v1.2.3)
#
# Stages:
#   1. build-backend: PyInstaller compiles RB_Backend for each platform
#   2. build-tauri:   Tauri bundles the app (uses backend artifact from stage 1)
#   3. publish:       tauri-action automatically creates a GitHub Release
#
# Platforms covered:
#   - Windows (x64)        → .msi + .exe installer
#   - macOS (Apple Silicon)→ .dmg (untested by maintainer, builds best-effort)
#   - Linux (x64)          → .AppImage (universal, runs on Ubuntu + Arch + Fedora)
#                          → .deb (Debian/Ubuntu)
#
# Cost: ~45 minutes CI per release (free tier covers ~40 releases/month)

name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:  # manual trigger from Actions tab

# Required permissions for tauri-action to create releases
permissions:
  contents: write
  id-token: write       # OIDC for future Sigstore signing (Schicht B)
  attestations: write   # SLSA provenance attestation

jobs:
  # ============================================================
  # STAGE 1 — Build Python Backend (PyInstaller)
  # ============================================================
  build-backend:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'windows-latest'
            target: 'x86_64-pc-windows-msvc'
            ext: '.exe'
          - platform: 'macos-latest'
            target: 'aarch64-apple-darwin'
            ext: ''
          - platform: 'ubuntu-22.04'
            target: 'x86_64-unknown-linux-gnu'
            ext: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python 3.11
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install Linux build deps (audio libs)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libsndfile1-dev \
            libsndfile1 \
            ffmpeg \
            libffi-dev

      - name: Install macOS build deps
        if: matrix.platform == 'macos-latest'
        run: |
          brew install libsndfile ffmpeg

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller==6.11.1
          pip install -r requirements.txt

      - name: Build backend with PyInstaller
        run: pyinstaller --clean backend.spec

      - name: Rename binary with platform triple
        shell: bash
        run: |
          mkdir -p artifacts
          if [ "${{ matrix.ext }}" = ".exe" ]; then
            mv "dist/RB_Backend.exe" "artifacts/RB_Backend-${{ matrix.target }}.exe"
          else
            mv "dist/RB_Backend" "artifacts/RB_Backend-${{ matrix.target }}"
            chmod +x "artifacts/RB_Backend-${{ matrix.target }}"
          fi
          ls -la artifacts/

      - name: Upload backend artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-${{ matrix.target }}
          path: artifacts/RB_Backend-${{ matrix.target }}*
          retention-days: 7

  # ============================================================
  # STAGE 2 — Build Tauri App (uses backend artifacts from stage 1)
  # ============================================================
  build-tauri:
    needs: build-backend
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'windows-latest'
            target: 'x86_64-pc-windows-msvc'
            args: ''
          - platform: 'macos-latest'
            target: 'aarch64-apple-darwin'
            args: '--target aarch64-apple-darwin'
          - platform: 'ubuntu-22.04'
            target: 'x86_64-unknown-linux-gnu'
            # Skip AppImage on Linux — bundling downloads linuxdeploy from GitHub
            # which frequently hangs in CI. Build only .deb + .rpm (universal-enough)
            # AppImage can be built locally if needed.
            args: '--bundles deb,rpm'

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'frontend/package-lock.json'

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            src-tauri/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Install Linux build deps (Tauri + WebKit + Audio)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          # libayatana-appindicator3-dev replaces deprecated libappindicator3-dev
          # libasound2-dev required by cpal (audio engine, transient via alsa-sys)
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            libgtk-3-dev \
            libasound2-dev \
            libjack-jackd2-dev \
            libpulse-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev

      - name: Install npm dependencies (root)
        run: npm ci

      - name: Install npm dependencies (frontend)
        working-directory: frontend
        run: npm ci

      - name: Download backend artifact
        uses: actions/download-artifact@v4
        with:
          name: backend-${{ matrix.target }}
          path: src-tauri/binaries/

      - name: Make backend executable (Unix)
        if: matrix.platform != 'windows-latest'
        run: chmod +x src-tauri/binaries/RB_Backend-*

      - name: List backend binary
        shell: bash
        run: ls -la src-tauri/binaries/

      - name: Generate macOS icon (icon.icns) if missing
        if: matrix.platform == 'macos-latest'
        working-directory: src-tauri/icons
        run: |
          if [ ! -f icon.icns ]; then
            echo "Generating icon.icns from icon.png..."
            mkdir -p icon.iconset
            sips -z 16 16     icon.png --out icon.iconset/icon_16x16.png
            sips -z 32 32     icon.png --out icon.iconset/icon_16x16@2x.png
            sips -z 32 32     icon.png --out icon.iconset/icon_32x32.png
            sips -z 64 64     icon.png --out icon.iconset/icon_32x32@2x.png
            sips -z 128 128   icon.png --out icon.iconset/icon_128x128.png
            sips -z 256 256   icon.png --out icon.iconset/icon_128x128@2x.png
            sips -z 256 256   icon.png --out icon.iconset/icon_256x256.png
            sips -z 512 512   icon.png --out icon.iconset/icon_256x256@2x.png
            sips -z 512 512   icon.png --out icon.iconset/icon_512x512.png
            sips -z 1024 1024 icon.png --out icon.iconset/icon_512x512@2x.png
            iconutil -c icns icon.iconset
            rm -rf icon.iconset
            echo "Generated icon.icns ($(ls -la icon.icns))"
          fi

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Sigstore signing will be enabled in Schicht B
          # TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          # TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Release ${{ github.ref_name }}'
          releaseBody: |
            ## Music Library Manager — ${{ github.ref_name }}

            Multi-platform automated build via GitHub Actions.

            ### Downloads
            - **Windows**: `.msi` installer
            - **macOS**: `.dmg` (Apple Silicon, unsigned — Gatekeeper warning expected)
            - **Linux**: `.AppImage` (universal, works on Ubuntu + Arch + Fedora) or `.deb` (Debian/Ubuntu)

            ### First-time install
            - **Windows**: SmartScreen warning is expected (no code-signing cert yet) — click "More info" → "Run anyway"
            - **macOS**: Gatekeeper will block — right-click → Open → confirm. (Or: `xattr -d com.apple.quarantine /path/to/app`)
            - **Linux**: `chmod +x *.AppImage && ./Music\ Library\ Manager.AppImage`

            See [SECURITY.md](https://github.com/SutobeHD/LibraryManagementSystem/blob/main/docs/SECURITY.md) for verification details.
          releaseDraft: true
          prerelease: ${{ contains(github.ref_name, 'alpha') || contains(github.ref_name, 'Alpha') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'Beta') || contains(github.ref_name, 'rc') }}
          args: ${{ matrix.args }}

  # ============================================================
  # STAGE 3 — Publish draft release after all builds succeed
  # ============================================================
  publish:
    needs: build-tauri
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Publish draft release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Wait briefly for all draft uploads to settle
          sleep 10
          # Find the draft release matching this tag and publish it
          gh release edit "${{ github.ref_name }}" \
            --repo "${{ github.repository }}" \
            --draft=false

