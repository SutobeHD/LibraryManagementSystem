# --- Security Defaults (Schicht A) ---
# Mirrors root .npmrc — frontend has its own scope

ignore-scripts=true
save-exact=true
audit-level=high
fund=false
update-notifier=false
prefer-offline=true
engine-strict=true
