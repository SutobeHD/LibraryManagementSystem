{
  "name": "library-management-system",
  "version": "1.0.0",
  "description": "LibraryManagementSystem — Rekordbox library editor with native audio engine",
  "main": "index.js",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "cleanup": "powershell -Command \"try { Get-NetTCPConnection -LocalPort 8000,5173 -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue } } catch {}; exit 0\"",
    "dev:full": "npm run cleanup && concurrently \"npm run dev --prefix frontend\" \"python -m app.main\"",
    "build": "npm run build --prefix frontend",
    "tauri": "tauri",
    "audit": "npm audit --audit-level=high && npm audit signatures",
    "lint:lockfile": "npx --yes lockfile-lint --path frontend/package-lock.json --type npm --validate-https --allowed-hosts npm --validate-package-names",
    "e2e:driver": "powershell -ExecutionPolicy Bypass -File tests/e2e/run-driver.ps1",
    "e2e:test": "npm test --prefix tests/e2e",
    "e2e:install": "npm install --prefix tests/e2e",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@tauri-apps/cli": "2.11.1",
    "concurrently": "9.2.1"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.0"
  }
}
